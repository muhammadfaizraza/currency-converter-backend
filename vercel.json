{
  "version": 2,
  "builds": [
    {
      "src": "src/server.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "destination": "/"
    }
  ],
  "buildCommand": "npm run build",
  "build": {
    "env": {
      "DBENV": "PRODUCTION"
    }
  }
}
